{% extends 'base.html.twig' %}

{% block title %}Espace Utilisateur{% endblock %}

{% block navbar %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="{{ path('user_home') }}">Bibliothèque</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarUser" aria-controls="navbarUser" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarUser">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link" href="{{ path('user_home') }}">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('user_livre_index') }}">Livres</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('livre_search') }}">Rechercher</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('panier_index') }}">Panier</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('commande_mes_commandes') }}">Commandes</a></li>
            </ul>
            <form method="post" action="{{ path('app_logout') }}">
                <button class="btn btn-outline-light">Déconnexion</button>
            </form>
        </div>
    </div>
</nav>
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row align-items-center">
        <div class="col-md-6">
            <h1 class="mb-4">Bienvenue dans votre espace personnel 📚</h1>
            <p class="lead">Consultez notre collection, ajoutez des livres à votre panier, passez vos commandes et explorez de nouvelles lectures !</p>
            <a href="{{ path('livre_search') }}" class="btn btn-primary btn-lg mt-3">
                🔍 Rechercher un livre
            </a>
        </div>
        <div class="col-md-6 text-center">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135755.png" alt="Espace utilisateur" class="img-fluid" style="max-height: 300px;">
        </div>
    </div>
</div>
 <div class="row justify-content-center">
        <!-- Livres -->
        <div class="col-md-4 mb-4">
            <div class="card text-center shadow border-0 h-100">
                <div class="card-body">
                    <i class="bi bi-book-half display-4 text-primary mb-3"></i>
                    <h5 class="card-title">Liste des Livres</h5>
                    <p class="card-text">Consultez tous les livres disponibles dans notre collection.</p>
                    <a href="{{ path('user_livre_index') }}" class="btn btn-outline-primary">Voir les livres</a>
                </div>
            </div>
        </div>

        <!-- Panier -->
        <div class="col-md-4 mb-4">
            <div class="card text-center shadow border-0 h-100">
                <div class="card-body">
                    <i class="bi bi-cart4 display-4 text-success mb-3"></i>
                    <h5 class="card-title">Mon Panier</h5>
                    <p class="card-text">Visualisez les livres que vous avez ajoutés à votre panier.</p>
                    <a href="{{ path('panier_index') }}" class="btn btn-outline-success">Voir le panier</a>
                </div>
            </div>
        </div>

        <!-- Commandes -->
        <div class="col-md-4 mb-4">
            <div class="card text-center shadow border-0 h-100">
                <div class="card-body">
                    <i class="bi bi-box-seam display-4 text-warning mb-3"></i>
                    <h5 class="card-title">Mes Commandes</h5>
                    <p class="card-text">Consultez l’historique de vos commandes et leurs détails.</p>
                    <a href="{{ path('commande_mes_commandes') }}" class="btn btn-outline-warning">Voir mes commandes</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}


